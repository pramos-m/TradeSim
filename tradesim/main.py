from tradesim import app # Import the already configured app from the package

if __name__ == "__main__":
    # Assuming Reflex >= 0.5.0, use app.run(). Use app.start() for older versions.
    app.run() 